{
  "name": "clock",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.5"
  },
  "dependencies": {
    "date-fns": "^2.28.0",
    "normalize.css": "^8.0.1"
  }
}
